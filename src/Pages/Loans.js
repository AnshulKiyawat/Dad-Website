import React from 'react';
import Header from '../Components/Header';

export default class Loan extends React.Component {
    render(){
        return(
            <div>
                <Header/>
                Loans UnderContruction
            </div>
        )
    }
}